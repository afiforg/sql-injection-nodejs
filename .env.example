# Database (required) — PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=secret
DB_NAME=sqlivuln

# Redis (optional - leave empty to run without cache)
REDIS_ADDR=

# Server
PORT=8080
